<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="AppTheme_PlaceHolder" parent="Theme.MaterialComponents.NoActionBar">        <!-- Primary brand color. -->
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Add more customizations as needed -->
    </style>
    <style name="MyHintTextAppearance" parent="TextAppearance.Design.Hint">
        <item name="android:textColor">@color/Blue</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="CustomAlertDialogStyle" parent="Theme.MaterialComponents.Light.Dialog.Alert">
        <item name="android:textSize">46sp</item> </style>
</resources>